config:
  target: "http://petclinic.localhost"
  phases:
    - duration: 600 # 10min 
      arrivalRate: 2 # 2 users
scenarios:
  - flow:
    - get:
        url: "/"
    - get:
        url: "/vets.html"
    - get:
        url: "/owners/find"
